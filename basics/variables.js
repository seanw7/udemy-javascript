// Can't define a variable more than once
let petName = 'Hal'
/* There are rules related to variable names
let petName = 'Spot'
*/
// Your variable name has to start with a letter 'a-z', can also start with '$' and '_'
petName = 'Spot'
// variable names cannot be reserved keywords
console.log(petName)